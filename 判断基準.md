# AGENTS.md — Purge Issue対応AI向け判断基準

このファイルは、Issue対応を自動化するAIエージェント（GitHub Actions上でGemini API等を呼び出す構成を想定）が
修正方針を判断する際に参照するルール集。単なる禁止事項の列挙ではなく、「なぜその設計になっているか」も
併記し、AIが設計思想を踏まえた提案をできるようにすることを目的とする。

## 基本方針
- 迷ったら安全側に倒す。調査不足のまま変更を加えることを最も避けたい。
- 判断がつかない・設計思想に触れる・トレードオフが生じる変更は、必ず人間確認を挟む（PRを作成するが自動マージはしない）。

## 絶対に自動マージしない変更（必ず人間レビュー）
- レジストリの書き込み・削除を伴う変更
- USN Journal操作に関わる変更（`MftSearchEngine.cs`, `ResidueScanner.cs` 等）
- 管理者権限（requireAdministrator）の要否に関わる変更
- ライセンス認証・課金ロジックへの変更（`LicenseKeyVerifier.cs`, `LicenseState.cs` 等）

## 自動対応してよい変更（叩き台作成→PR→人間の最終承認は必要）
- タイポ、コメント追加
- ログ出力の追加
- 既存関数のリファクタ（外部から見た挙動が変わらないもの）
- テストコードの追加

## Issue種別ごとの確定判断

### 残存物検出の誤検知
まず残存スキャンのロジック強化で対応する。MftSearchEngineはEverything（ファイル検索ソフト）と
同じ方式（USN Journal / MFT直読み）のため、検出精度は原理的に十分出せるはず、という前提に立つ。
検出ロジックを「厳しくする」か「緩くする」かで二択になる場合は、誤検知の実例と合わせてPRの説明に
両方の選択肢とトレードオフを明記し、人間の最終判断を仰ぐこと。

### 管理者権限なしの簡易モード要望
却下する。管理者権限必須の設計は維持する（USN Journal / レジストリ操作に必須のため）。
関連する要望が来た場合は、設計上の制約を説明した上でクローズしてよい。

### Windowsバージョン間の挙動差（10/11など）
開発者の所有環境（実機で動作確認できるOS）を優先する。実機で検証できない環境向けの修正は、
コードだけで確信が持てない場合は必ず人間確認に回す。Mac非対応も同じ理由（実機で確認できない
プラットフォームには対応しない）。

### エラーメッセージ等のテキスト方針
「わかりやすさ」と「技術的正確さ」はどちらか一方を選ぶのではなく、両方を満たす形（併記可能な形）で
書く。片方を犠牲にする書き方は避ける。

### 多言語対応
優先順位は 日本語 > 英語 > その他。急ぐ必要はない。英語以外の言語追加要望は、対応できる範囲で
気長に取り組む温度感でよい。翻訳の質・ニュアンス判断はAI単独で確定せず、人間確認を挟む。

## 設計経緯（判断の背景として参照）
- 配布形式はWiXベースのMSIインストーラー。Velopackは「管理者権限前提のアプリではバックグラウンド
  自動更新が技術的に無理（UAC同意が毎回必要）」という理由で撤退済み。Velopack関連の提案は不要。
- Microsoft Store配信は行わない。requireAdministrator設計と競合するため。
- アップデート確認先はGitHub Releases。この方針は変更しない前提でよい。
- 課金基盤はStripe + Cloudflare Workers。返金不可だが、ライセンスキー再入力でPC入れ替え後の復旧は
  可能な設計。この復旧経路を壊す変更は必ず人間確認に回す。

## 運用構成（このファイルを読む側の前提）
- Issue登録 → GitHub Actions（GitHub-hosted runner）→ Gemini API（無料枠）で修正案・PR作成、という
  構成を想定。インフラ・推論ともに無料枠内で完結させる方針のため、有料の判断特化モデル等は
  組み込まない（検討したが却下：TypeSafe社のJev等）。
- 判断基準・過去の判断ログの一次ソースは開発者のObsidian Vault（全体的な判断基準）およびこのリポジトリ
  （プロジェクト固有の判断基準）に分かれる。このファイルは後者にあたる。
