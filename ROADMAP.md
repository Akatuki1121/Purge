# UninstallTool 開発ロードマップ

## ゴール
残存ファイル検出精度の高いWindowsアンインストーラーを作る。
常駐なし、外部ツール依存なし(単一exe)、無料版/有料版で機能分割。

## 現在地(2026-09-04)

### 一言でいうと
**主要機能の実装は完了。現在は、安全性の仕上げと配布品質の向上の段階。**

### 最終目標に対する達成状況
| 目標 | 状況 | 現在の内容 |
|---|---|---|
| 高精度な残存物検出 | 実装済み | レジストリ、サービス、タスク、PATH、スタートアップ、MFTを横断。NameMatcherと発行元情報で誤検出を抑制 |
| 安全に削除できる | 実装済み | 既定ドライラン、選択削除、検出根拠/確度表示、削除前JSONマニフェスト、レジストリ.regバックアップ |
| 快適に使える | 実装済み | 非同期処理、検索、進捗表示、キャンセル、空状態表示、操作ログ詳細パネル |
| 常駐なし・外部ツール依存なし | 実装済み | 常駐プロセスなし。USN Journalは自前実装。サービス/タスク削除時のみWindows標準コマンドを利用 |
| 無料版/有料版の分離 | 基盤実装済み | Proゲートと開発用アンロックは実装済み。正式なライセンス検証は未着手 |
| 配布できる単一exe | 未着手 | self-contained / Native AOTの互換性検証が必要 |

### 残作業の優先順位
1. **安全性**: ファイル・サービス・タスクの復元、SelfUninstaller実機テスト
2. **速度と操作性**: MFT検索の実進捗率、対象絞り込み、必要なら段階表示
3. **配布品質**: 単一exe化、GitHub Actionsでのビルド/テスト/成果物作成
4. **製品化**: 正式ライセンスキー検証、決済導線

### 状態の読み方
- `実装済み`: コードと自動テスト、またはUIビルドで確認済み
- `実機確認待ち`: 破壊的操作や管理者権限を伴うため、配布用コピーなどで確認が必要
- `未着手`: 設計または実装がまだない項目

## 実装順序

### Phase 0: 基盤 [完了]
- [x] OperationLog: 操作ログ/状態トラッカー、エラー時に文脈付きレポートを自動生成
- 動作確認済み(2026-08-16)

### Phase 1: インストール済みアプリ一覧取得 [完了]
- [x] レジストリ `HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall`
- [x] `HKLM\Software\WOW6432Node\...\Uninstall` (32bitアプリ)
- [x] `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall` (ユーザー単位インストール)
- [x] 各アプリの DisplayName, DisplayVersion, Publisher, InstallLocation, UninstallString を取得
- [x] OperationLogに各ステップを記録
- 動作確認済み(2026-08-16): 実機で112件のアプリを正しく検出

### Phase 2: 通常アンインストール実行 [完了]
- [x] 選択したアプリの UninstallString を実行(既存の公式アンインストーラーを呼ぶ)
- [x] 完了待機、終了コード確認
- [x] OperationLogに記録
- [x] 安全のためdryRunフラグを実装(既定true)。テスト用使い捨てアプリができるまで実行はしない
- 動作確認済み(2026-08-16): 実機の112アプリ中10件でUninstallStringパースをドライラン確認、複雑なパス(空白入り・引数付き)も正しく分解できることを確認

### Phase 3: 簡易UI (WPF) [完了]
- [x] プロジェクトを UninstallTool.Core(ロジック) / UninstallTool.UI(WPF) / UninstallTool(コンソール、テストランナー) の3つに分離
- [x] アプリ一覧表示(Phase1のAppInventoryをそのまま利用)
- [x] 一覧から選択 → アンインストールボタン(Phase2のAppUninstallerをそのまま利用、UIのチェックボックスでdryRun切替可能)
- [x] 操作ログをウィンドウ内に表示
- 動作確認済み(2026-08-16): 実機でウィンドウ起動・アプリ選択・ドライラン実行(結果ダイアログ「DryRun」表示)を確認
- Geek Uninstaller相当のシンプルな操作感を最低限で実現
- この時点ではまだ残存物検出機能はなし(見た目だけ先行)

### Phase 4: MFT高速検索エンジン [完了]
- [x] USN Journal (`FSCTL_ENUM_USN_DATA`) を使った自前実装(P/Invoke、外部依存ゼロ)
- [x] 管理者権限チェック、非対応ボリュームのハンドリング(警告ログを出して安全にスキップ)
- [x] アプリ名/パブリッシャー名でファイルパスを高速検索(部分一致、大文字小文字無視)
- [x] ParentFileReferenceNumberを辿ったフルパス復元、循環参照の安全な検知
- 動作確認済み(2026-08-16、管理者権限で実機実行): C:ドライブ全体(273万9929件)を28秒でスキャン、検索語「Obsidian」で94件を正しいフルパスで検出。深い階層・空白入りパスも正確に復元できることを確認

### Phase 5: 残存物スキャン(横断)+ 選択削除 [実装完了、実機確認待ち]
- [x] AppData / LocalAppData / ProgramData 配下の関連フォルダ(MFT検索経由、オプトイン)
- [x] レジストリ全キー横断検索(Uninstallキー以外も、Software配下)
- [x] サービス登録の確認
- [x] タスクスケジューラのエントリ確認(レジストリTaskCache経由)
- [x] 環境変数PATHの該当パス確認
- [x] スタートアップ項目(レジストリRun + スタートアップフォルダ)確認
- [x] ResidueRemover: カテゴリごとの削除実装(レジストリ削除/sc.exe delete/schtasks.exe delete/PATH編集/ファイル削除)、既定dryRun
- [x] UI: 「残存物をスキャン」ボタン→チェック付き一覧ウィンドウ(全選択/全解除/選択削除)、MFT検索は重いためオプトインチェックボックス+非同期実行
- ビルド成功(2026-08-16) — 実機での動作確認待ち(コンソール版でのScanAll確認、UI版でのスキャン→選択削除の一連の流れ確認)
- 動作確認済み(2026-08-16、管理者権限で実機実行): 架空アプリ名で全カテゴリ横断スキャンを実行し0件(誤検出なし)を確認

### Phase 6: 孤児候補の相関ロジック [実機確認済み、実用レベルまで精度改善]
- [x] MftSearchEngineに ListSubdirectories を追加(指定フォルダ直下の子フォルダを高速列挙)
- [x] OrphanDetector: Program Files / Program Files (x86) / AppData\Roaming / AppData\Local 直下を走査
- [x] インストール済みアプリの InstallLocation と名前ベースで突き合わせ、一致しないものを孤児候補として抽出
- [x] Windows自体の既知フォルダ(Microsoft, Packages, WindowsApps等)は除外リストで除外
- [x] 最終アクセス日時のような曖昧な指標は不採用(誤検出が多いとの指摘を踏まえた設計判断)
- [x] 削除は行わず候補提示のみ(誤検出リスクの高いカテゴリのため)
- 動作確認済み(2026-08-16、管理者権限で実機実行): C:ドライブ全体を走査、初回48件検出
- 誤検出の原因調査と修正(2026-08-16):
  - [x] 短い名前(例: "Git")が部分一致の最小文字数制限で無条件除外される不具合を修正(短い名前同士は完全一致のみ許可する救済ロジックに変更)
  - [x] トークン単位の部分一致を追加(例: "Arduino15" と "Arduino IDE 2.3.10" のように文字列として互いを含まないが単語を共有するケースを救済。キャメルケース・数字境界で分割、4文字未満/数字のみのトークンは除外)
  - [x] OneDriveをKnownNonAppFolderNamesに追加(per-user配布のためUninstallレジストリキーに登録されず、名前相関では原理的に検出不可能と判明)
  - [x] DisplayNameとフォルダ名が単語レベルで一切重ならないケース(例: "Epic Games Launcher" と "UnrealEngine")向けに、キュレート済みエイリアス表(KnownAppFolderAliases)を追加
  - [x] さらに拾いきれないケース向けに、フォルダ内exe/dllのFileVersionInfo(CompanyName/ProductName)を既知アプリのDisplayName/Publisherとトークン一致させる2段目チェックを実装(includeExecutableMetadataCheck、オプトイン)。名前相関で除外しきれなかった候補のみに適用することで速度への影響を抑制
- 動作確認済み(2026-08-16、管理者権限で実機実行): 修正後は48件→25件まで削減。Git/OneDrive/Fusion360/Arduino15/UnrealEngine系/Android等、現役インストール済みアプリの誤検出をすべて解消したことを確認
- 既知の残課題: レジストリに一切登録されない配布形態(OneDriveのようなケース)は個別に除外リストで対応するしかなく、汎用的な解決策はない(業界の他ツールでも確認できなかった限界と一致)

### Phase 7: UI拡張 + 自己アンインストール機能 [完了]
- [x] MainWindowに「孤児候補をスキャン(実験的)」ボタンを追加
- [x] OrphanWindow: 検出結果をチェック付き一覧表示、削除は既存のResidueRemover(MftFileカテゴリ)に委譲して一本化
- [x] 「エクスプローラーで開く」ボタンで削除前に中身を目視確認できる導線を追加(誤検出リスクへの安全策)
- [x] 管理者権限チェック(ElevationChecker)+ 未昇格時の警告バナー(サイレント0件事故の再発防止)
- [x] 操作ログをTextBox化してコピー可能に
- [x] SelfUninstaller: batファイル経由の自己削除実装(2段階確認ダイアログ付き)。BAT生成内容の自動テスト済み。実機削除は配布用コピーでリリース前に確認する
- ビルド成功(2026-08-16、Core/UI両方)

### Phase 8: 無料版/有料版の分岐実装 [実装完了]
- [x] LicenseState: Pro機能ゲートの窓口を一本化(現状は常にfalseを返すスタブ、将来ライセンスキー検証に差し替え可能な設計)
- [x] AppListViewを複数選択可能に変更(SelectionMode=Extended)
- [x] 「複数アプリを一括アンインストール(Pro)」ボタン: LicenseState.IsProUnlockedがfalseの間は案内のみ表示、実行しない
- [x] 無料版: 1アプリずつのフル機能(Phase1〜7全部込み)は従来通りすべて利用可能
- ビルド成功(2026-08-16、Core/UI両方)
- [x] スキャン結果エクスポート(将来のPro機能候補、残存物/孤児候補のCSV出力を実装済み)
- [x] 除外リスト保存(将来のPro機能候補、孤児候補向けを実装済み)
- 未着手: 実際のライセンスキー発行・検証の仕組み(配布形態と課金方式の決定後に着手)

## 今後の実装フェーズ

### Phase 9: 安全性の仕上げ [一部完了]
- [x] 検出結果への検出根拠・確度表示
- [x] 削除前JSONマニフェスト保存
- [x] レジストリ削除前の.regバックアップ
- [x] ファイル/フォルダの削除前スナップショット保存と復元
- [x] レジストリの.regバックアップからの復元
- [x] サービス/タスク定義の削除前記録(自動復元は未対応)
- [ ] SelfUninstallerの配布用コピーによる実機テスト

### Phase 10: 速度・操作性の仕上げ [進行中]
- [x] 残存物スキャンのUI非同期化
- [x] スキャン中のキャンセルと現在処理の表示
- [x] アプリ一覧の検索・空状態表示
- [x] MFT検索の走査済み件数表示とフルパス解決キャッシュ(全体件数を取得できないため、百分率ではなく実件数を表示)
- [x] MFT検索の対象ルート指定API(パス境界を考慮して対象外を除外)
- [x] 大量結果のListView仮想化(Recycling)
- [ ] 大量結果の段階表示またはページング

### Phase 11: 配布品質 [一部完了]
- [x] self-contained単一exeの発行検証(win-x64発行成功。成果物はActionsで保存する)
- [x] GitHub Actionsでrestore・Releaseビルド・テスト・self-contained成果物保存を自動化
- [x] Native AOTの適用可否を検証(WPFのトリミング非対応によりNETSDK1168。self-contained方式を採用)
- [ ] リリース用署名、ライセンス同梱、アップデート方針を決定

### Phase 12: 製品化・課金 [実装完了、本番運用移行済み]
- [x] ライセンスキーの署名検証方式を実装(RSA、オフライン完結、サーバー不要)
  - LicenseKeyVerifier(Core): 公開鍵のみ埋め込み。秘密鍵は絶対に含めない設計
  - LicenseKeyIssuer(開発者専用、非配布プロジェクト): 鍵ペア生成・キー発行のコンソールツール
  - LicenseState: dev_unlock.flagに加え、%LocalAppData%\Purge\license.keyへの保存・検証に対応
  - LicenseWindow(UI): メニューから入力・認証・解除・「購入はこちら」導線
  - 自動テスト6件追加(正常系、改ざん検知、不正形式)。全50件成功を確認
- [x] 決済導線: Stripe(Managed Payments、3.5%/取引、税務・不正対策・サポートをStripe側に一任)+ Payment Link
- [x] 決済完了→ライセンス自動発行の仕組み: Cloudflare Worker(PurgeLicenseServerプロジェクト)がStripeのWebhook(checkout.session.completed)を受信し、LicenseKeyIssuerと同じRSA署名ロジックでキーを発行、SendGrid経由でメール送信(Resendから切替済み)
- [x] 価格確定: ¥2,980(税込)、買い切り(無期限)。競合調査(Geek Uninstaller Pro $24.95が最も近い比較対象)を踏まえて設定
- [x] 返金ポリシー確定: 返金不可。PC買い替え・再インストール時はメールで送付済みの同じキーを再入力すれば復旧可能
- [x] セキュリティインシデント対応: 秘密鍵をチャット上に誤表示した事故を受け、発行実績ゼロの段階で鍵をローテーション(再生成)。Core側公開鍵・テストのサンプルキー・Cloudflare側のシークレットすべて新しい鍵に更新済み
- [ ] 特定商取引法の表記: テンプレート作成済み(Obsidian Vault内)、氏名・メールアドレス等の個人情報欄が未記入
- [ ] 本番Stripeアカウントの最終確認(個人事業主登録・本人確認・銀行口座登録)
- [ ] エンドツーエンドの購入テスト(テストカードでの一連の動作確認)が未実施
- [ ] LicenseState.PurchaseUrlが現状テストモードのPayment Linkのまま。本番リンクへの差し替えが必要

### Phase 13: 配布パッケージング [方針転換: Velopack撤去、WiXベースのMSIインストーラーを採用]
- [x] (2026-09-12) Velopackを一時採用したが、Velopackの自動更新は「バックグラウンドで気づかれずに更新する」設計が前提であり、本アプリは管理者権限(requireAdministrator)で起動するため更新のたびに必ずUAC同意が要ることが判明。この前提と自動更新の相性が悪いと判断し撤去(UI/Purge.UI.csprojからVelopackパッケージ参照を削除、App.xaml.cs内のVelopack関連コードを撤去)
- [x] (2026-09-12〜13) WiXベースのMSIインストーラーに回帰。installer/フォルダを再作成し、以下を解決:
  - Files要素でpublish/Purge配下を再帰的に取り込み、手動でのファイル一覧列挙を回避
  - ComponentGroupRefのID不正エラー(Files要素の自動生成IDにパス文字列がそのまま使われ不正な識別子になる)→ComponentGroup要素を明示してId/Directoryを指定する形に修正
  - 日本語文言(WIX0311: コードページ1252に日本語が含まれない)→Package要素にCodepage="65001"を追加
  - ICE80(32bit/64bit不整合: self-contained発行のexeは64bitだが既定でコンポーネントが32bit扱いになる)→.wixprojにPlatforms=x64を追加、dotnet build -p:Platform=x64でビルド
  - 最終的にPurge.Installer.msi(約55MB)の生成に成功
- [x] Product.wxsのバージョンをハードコードから`$(var.ProductVersion)`変数化、wixprojでMSBuildプロパティ経由(`-p:ProductVersion=`)で注入できるように変更。ローカルで`-p:ProductVersion=9.9.9`指定のビルドが成功することを確認
- [x] GitHub Actions(release.yml)をVelopack向けからWiX/MSIビルドへ書き換え完了(dotnet publish→dotnet build installer/Purge.Installer.wixproj→MSIをバージョン付きファイル名にリネーム→softprops/action-gh-releaseでGitHub Releasesへ添付)
- [x] スタートメニュー/デスクトップショートカットの動作確認、アンインストール時の挙動確認(MSI標準機能のため未検証) → 2026-09-14実機確認済み。ローカルビルドのMSIをmsiexec /qnで実行し、スタートメニュー(`C:\ProgramData\...\Purge.lnk`)・共通デスクトップ(`C:\Users\Public\Desktop\Purge.lnk`)の両方に正しくショートカットが作成されることを確認。per-machineインストールのため個人デスクトップでなく共通デスクトップに作成される仕様(エクスプローラー上は自分のデスクトップと統合表示される)
- [x] リリース用のコード署名を検討(2026-09-13)。安価なAzure Artifact Signing(旧Trusted Signing、月$9.99)は個人開発者向けが現状米国・カナダ限定のため利用不可と判明。残る従来型OV証明書は年$100〜400程度かかるため、売上実績のない初回リリースでは見送りと決定。**当面は無署名でリリースし、SmartScreen警告が出ることをリリースノートで案内する。売上が育ってから証明書購入を再検討する**方針
- [x] 自動更新機構: 「バックグラウンドで気づかれずに更新」は管理者権限起動(requireAdministrator)である以上不可能(更新のたびに必ずUAC同意が要る)と判明済み。代わりに「半自動更新」を実装(2026-09-17完了): 起動時に新バージョンをバックグラウンドでチェック→控えめなバナーで通知(MainWindow)→「ダウンロード」ボタンでMSIをアプリ内ダウンロード(進捗表示付き)→完了後の確認ダイアログでユーザーがインストーラー起動を選べば、explorer.exe経由で起動しアプリを終了。ダウンロード失敗時はRelasesページへの手動導線も維持。実機での一連の動作確認は未実施

## 技術メモ
- .NET 8 / C# / WPF
- 単一exe化(自己完結デプロイ)を最終的に検討
- 管理者権限が必要な処理(USN Journal等)はマニフェストで要求するか、実行時にUAC昇格を促す設計にする

## 進捗ログ
- 2026-08-16: Phase 0完了。OperationLogの動作確認済み(意図通りエラー文脈を生成できることを確認)
- 2026-08-16: Phase 5/6を実機で初確認(管理者権限必須と判明)。Phase6で孤児候補48件検出、うちGit/OneDrive/Fusion360/Arduino15/UnrealEngine系等の現役アプリ誤検出を特定
- 2026-08-16: 誤検出修正(短い名前の完全一致救済、トークン一致、既知除外リスト拡充、エイリアス表、exe/dllメタデータによる2段目チェック)を実装・実機確認。48件→25件に削減、既知の誤検出はすべて解消
- 2026-08-16: Phase 7(孤児候補UI統合、管理者権限チェック+警告バナー、ログのコピー対応、自己アンインストール)完了
- 2026-08-16: Phase 8(複数選択+一括アンインストールのPro機能ゲート)完了。全Phase(0〜8)の実装が完了
- 2026-08-16: アプリ一覧にアイコン表示追加(AppIconResolver: DisplayIcon→UninstallStringのexe→InstallLocation内のexeの順に探索)
- 2026-08-16: 開発者向けProアンロック追加(LicenseState: exeフォルダのdev_unlock.flagで自分だけPro機能を使える。配布物には含めない)
- 2026-08-16: 孤児候補にサイズ・ファイル数・最終更新日時を追加(OrphanCandidate.FileCount/TotalSizeBytes/LastModified)。判断材料として提示するのみで自動判定には使わない
- 2026-08-16: UIデザインを試行錯誤(ダークテーマ+カード形式→見やすさ優先で明るい標準テーブル形式に最終決定。Geek Uninstaller等の見慣れたレイアウトに寄せた)
- 2026-08-17: WPF-UI(lepoco/wpfui、MITライセンス)導入、Fluent Design化。メニューバー(ファイル/表示/ツール/孤児候補スキャンへの導線含む)追加。THIRD_PARTY_LICENSES.md作成(未同梱)
- 2026-08-17: 画面遷移をユーザー目線で見直し。孤児候補スキャンボタンをメイン画面から撤去しメニュー「ツール」に一本化、単体アンインストール後に残存物スキャンを自動提案する流れに変更、一括アンインストールボタンは複数選択時のみ表示に変更
- 2026-08-17: 操作ログにコピーボタン追加(範囲選択に頼らない)
- 2026-08-18: 起動パフォーマンス改善(アイコン抽出・レジストリ読み取りの非同期化、Mica背景削除)
- 2026-08-18: 起動時クラッシュの原因判明・修正(ToggleSwitchのIsChecked初期値によりInitializeComponent実行中にChecked/Uncheckedハンドラが発火し、未初期化フィールド参照でNullReferenceException。nullガードで解消)。イベントログ調査で特定
- 2026-08-18: アプリアイコンにフォールバック追加(見つからない場合はshell32.dllの汎用アイコン)、開発者向けProアンロック(dev_unlock.flag)追加
- 2026-08-18: ResidueWindow/OrphanWindowもWPF-UI Fluent化、3画面の見た目を統一
- 2026-09-04: UI改善および安全性の強化
  - 残存物/孤児候補画面のチェックボックス依存を廃止し、ListView標準の行選択(Ctrl/Shift複数選択)へ移行
  - 単体アンインストール完了後、ダイアログを挟まず自動で残存物スキャン画面へスムーズに移行するよう変更
  - レジストリ/exe等の会社名(CompanyName)からのPublisher補完ロジック(AppPublisherResolver)を追加
  - NameMatcherをResidueScanner全体へ適用し、短いアプリ名での誤検出リスクを防止
  - THIRD_PARTY_LICENSES.mdのビルド時自動コピー設定を追加
  - バージョン情報ダイアログの文言整理
- 2026-09-04: GitHub Issueによる不具合報告・機能要望の送信画面を追加。不具合/要望の種別、概要、再現手順または利用場面、期待する動作を入力し、環境情報と任意の操作ログを添付できるようにした
- 2026-09-04: メイン画面の視覚階層を調整。アプリ一覧の通常文字色・選択/ホバー状態を整理し、列ヘッダーを統一、操作ログを初期状態で折りたたむようにした。UIビルド確認済み
- 2026-09-04: メイン画面を一覧中心に再構成。アプリ名・バージョン・発行元・インストール場所を横断検索できる検索欄と表示件数を追加し、孤児スキャン/自己アンインストールをメニューへ集約。UIビルド確認済み
- 2026-09-04: メイン画面のデザイン刷新を継続。メニューバーを操作ツールバーへ再構成し、アプリ一覧に読み込み中・アプリなし・検索結果なしの専用状態表示を追加。UIビルド確認済み
- 2026-09-04: 操作ログを画面下部の折りたたみ式詳細パネルへ再構成。ログの説明、コピー操作、診断情報を一覧の主操作と分離し、必要時だけ展開できるようにした
- 2026-09-04: 発行元表示を改善。DisplayIcon/UninstallString/インストール先から複数のexe候補を探索し、CompanyNameまたはProductNameを補完。情報を取得できない場合も空欄ではなく「発行元情報なし」と表示するようにした
- 2026-09-04: 残存物スキャンにCancellationTokenと進捗通知を追加。レジストリ・サービス・タスク・PATH・スタートアップ・MFTの走査中にキャンセルでき、メイン画面に現在の走査段階とキャンセル操作を表示するようにした
- 2026-09-04: 残存物の検出結果に確度と検出根拠を追加。削除前に「何が一致した候補か」を確認できるようにした
- 2026-09-04: MFT検索の進捗を走査済み件数で表示し、検索結果のフルパス解決に参照番号キャッシュを追加。テスト44件・Release UIビルド確認済み
- 2026-09-04: MFT検索に対象ルート指定APIを追加。既存の全体検索を維持しつつ、指定時はパス境界を考慮して対象配下だけを返すようにした。ソリューション全体ビルド・テスト44件確認済み
- 2026-09-04: win-x64 self-contained発行とGitHub Actionsの成果物保存を確認。Native AOTはWPFのトリミング非対応(NETSDK1168)のため採用せず、self-contained方式を継続
- 2026-09-04: 4プロジェクトをUninstallTool.slnxに登録。ソリューション全体のDebugビルドとテスト44件を確認し、VS Codeのプロジェクト認識入口を明示
- 2026-09-04: 実削除前に対象・確度・存在状態をJSONマニフェストへ保存し、レジストリ項目は.regへエクスポートする削除前バックアップを追加。バックアップ失敗時は削除を中止する
- 2026-09-04: 削除前バックアップにファイル/フォルダのスナップショットとサービス/タスク定義の記録を追加。ファイル/レジストリ復元とメイン画面の復元メニューも実装し、UIビルド・テスト44件を確認
- 2026-09-04: 孤児候補の除外リスト保存を追加。ユーザー単位のLocalAppData配下へJSON保存し、検出画面の「今後表示しない」から再スキャン以降の候補を除外できるようにした。自動テスト・UIビルド確認済み
- 2026-09-06: Phase 12(製品化・課金)着手。ライセンスキーのRSA署名検証方式を実装(LicenseKeyVerifier/LicenseKeyIssuer/LicenseState/LicenseWindow)。自動テスト6件追加、全50件成功
- 2026-09-06〜12: 決済導線を構築。製品名を「Purge」に決定(競合調査を踏まえ命名。虫眼鏡モチーフは既存の高速検索ソフトと被るため不採用、削除・掃除モチーフの独自アイコンを作成)。価格¥2,980・買い切り・返金不可で確定。Stripe Managed Payments(3.5%/取引)を採用し税務・不正対策・サポートを一任する方針に決定。Cloudflare Worker(PurgeLicenseServerプロジェクト)でStripe Webhook受信→ライセンスキー自動発行→メール送信の一連の自動化を構築。メール送信サービスはResendからSendGridへ切替
- 2026-09-11: セキュリティインシデント対応。RSA秘密鍵が別チャットで誤って平文表示された事故を受け、発行実績ゼロの段階で鍵をローテーション(再生成)。Core側公開鍵・テストのサンプルキー・Cloudflare側のシークレットをすべて新しい鍵に更新し、影響ゼロで収束
- 2026-09-12: リポジトリ・プロジェクト名をUninstallToolからPurgeへリネーム(GitHub・ローカルとも)。releases/・publish/を.gitignoreに追加してビルド成果物をリポジトリ管理から除外(フォルダ構造の整理)
- 2026-09-12: Velopackを一時導入しGitHub Actionsでの自動配布(v0.1.0)まで構築したが、requireAdministrator(常時管理者権限起動)のアプリではアップデートのたびに必ずUAC同意が必要になり、Velopackが前提とする「バックグラウンドで気づかれずに更新」という設計と相性が悪いと判明したため撤去を決定
- **【訂正】2026-09-12の同日中に、上記の方針転換を知らない別セッションが「installer/(WiX実装)は不要な残骸」と誤って判断し削除してしまう事故が発生。その直後、別セッションがWiXインストーラーの再構築に着手し、Product.wxsのビルドエラー(ComponentGroupRefのID不正、日本語コードページ、32/64bit不整合等)を解消してPurge.Installer.msiの生成に成功、installer/フォルダは復元済み。ただし`.github/workflows/release.yml`はVelopack時代のまま未修正で残っていたため、このタイミングでWiX/MSIビルドへ書き換える(詳細はPhase 13参照)
- 2026-09-13: ROADMAP.md内の矛盾した記述(直前の進捗ログが「Velopackで完全自動化を構築」、Phase 13が「WiXへ回帰」と正反対の内容だった)を発見・訂正。実態(installer/フォルダの存在、csprojにVelopack参照なし)を確認した上で、release.ymlをWiX/MSIビルドへ書き換え。Product.wxsのバージョンを`-p:ProductVersion=`で注入可能にし、ローカルでのpublish→WiXビルドの一連の成功を確認
- 2026-09-14〜15: Issue #1〜#11に対応(GitHub上でクローズ済み、詳細はリポジトリのPR履歴参照): 開始時のSmartScreen警告への案内、孤児候補→対応アプリ不明フォルダの表記統一漏れ修正、管理者権限起動時にStripe購入ページ(explorer.exe経由のURLオープンでUIPI制限を回避)、WiXインストーラーの日本語化、CSVエクスポートの購入導線統一、不具合報告への任意スクリーンショット添付、GitHub ActionsへのNuGetキャッシュ導入(ビルド時間短縮)
- 2026-09-16: UI/UX改善。(1)「その他」メニュー内にしかなかったPro導線が気づかれにくいとの指摘を受け、メイン画面ツールバーに常設の「Proにアップグレード」ボタンを追加(購入済みなら自動的に非表示)。(2)「ドライラン」という用語が直感的でないとの指摘を受け、MainWindow/ResidueWindow/OrphanWindow全3画面のトグルを「テスト実行(削除しない)」/「本番実行(削除する)」という動作がそのまま伝わる文言に統一。(3)自動更新について、管理者権限起動が前提のため完全なバックグラウンド自動更新は不可能(更新ごとに必ずUAC同意が要る)と改めて確認した上で、「新バージョンを検知→控えめな通知→ワンクリックでMSIダウンロード・起動」という半自動更新の方針を確定(実装は次回以降)
- 2026-09-16: 別セッションが5時間の利用上限に達したため引き継ぎ。未コミットのまま残っていた3点を検証・完成させてPR化: (1) App.xaml.csのAvailableUpdateMsiUrlが宣言のみで値を設定する処理が無い未完成状態だったため、GitHub Releases APIのassets一覧から.msi拡張子のファイルを探して設定する処理を追加。(2) installer/の多言語対応(Issue #12)下地としてLoc/ja-jp.wxl・en-us.wxl・License.ja-jp.rtf・License.en-us.rtfを追加、wixprojのCulturesをja-JP;en-USに拡張し、ローカルビルドでja-JP/en-US両方のMSIが正常に生成されることを確認(0エラー・0警告)。(3) ショートカット作成のオン/オフ切り替え(Issue #14)の下地として、Product.wxsのショートカットをAdvertise方式からINSTALLSTARTMENUSHORTCUT/INSTALLDESKTOPSHORTCUTプロパティによる条件付きComponentへ再構成(ただし値は現状常に1固定で、インストール中にユーザーが選択するWixUIカスタムダイアログは未実装のまま。Issue #14はクローズせず継続)。release.ymlもja-JP/en-US両方のMSIをGitHub Releasesにアップロードするよう更新済み

## 未実装・要対応(会話で挙がったが手つかずのもの)
- [x] 単体アンインストール後に残存物スキャンを自動提案 ではなく、提案なしで移行へ。 (完了)
- [x] アプリ等を消す際、チェックボックスを使わないようにする (完了: ListView行選択へ移行)
- [x] 単体アンインストール後の残存物スキャンで、選んだアプリと無関係な結果が混ざる懸念 (完了: NameMatcherをResidueScanner全体へ適用)
- [x] THIRD_PARTY_LICENSES.mdを作成したが、配布物(exeと同じフォルダ)に同梱する仕組みが未実装 (完了: csprojにCopyToOutputDirectoryを設定)
- [x] バージョン情報から、「Geek Uninstallerを超えることを目指した」を削除する (完了)
- [x] 複数(一括)アンインストール後の残存物スキャン自動提案(成功した各アプリを順次スキャンし、アプリ名付きで結果を表示)
- [x] SelfUninstallerの自動テスト(BAT生成内容を検証)。実機削除テストは配布用コピーでのみ実施するため、リリース前確認として継続
- [x] ビルド時のNU1510警告確認(2026-09-04のDebugビルドで警告なし。System.Drawing.Commonの直接参照もなし)
- [x] GitHub Issueによる不具合報告・機能要望の送信(入力フォーム、環境情報、任意の操作ログ、種別ラベル付きのIssue下書き)
- [x] 孤児候補の除外リスト保存(LocalAppData配下へのJSON保存と検出画面からの登録)
- [x] 残存物スキャンのUI非同期化・キャンセル・進捗表示(各走査ループとMFTページ境界でキャンセル可能)
- [x] 残存物の検出根拠・確度表示(カテゴリ別の確度と既存の一致理由を結果画面へ表示)
- [x] 削除前バックアップ/復元マニフェスト(JSON、レジストリ.reg、ファイル/フォルダスナップショット)

## 次にやるとよさそうなこと(未着手)
- [x] スキャン結果エクスポート(Pro機能候補、残存物/孤児候補のCSV出力を実装済み)
- [x] 除外リスト保存(Pro機能候補、孤児候補向けを実装済み)
- 実際のライセンスキー発行・検証の仕組み、決済導線
- 単一exe化(Native AOT等での軽量化、配布のしやすさ向上)
- [x] メイン画面のデザイン刷新(ツールバー、検索/フィルター、空状態、操作ログ詳細パネル)
- [ ] サービス・タスク定義の自動復元(定義の削除前記録とファイル/レジストリ復元は実装済み)
- Windows以外の環境での動作確認は対象外(このツールはWindows専用)



## リリース運用方針

**リリースは必ず内容ベースで判断する。「N件溜まったら」「週1回」のような
件数・期間ベースでは判断しない。**

- 致命的バグ(機能が完全に動かない系)は即リリース。他の作業完了を待たない
- 実機で未検証の変更(特にインストーラー周り)が残っている間はリリースしない
- 詳細は docs/WORKFLOW_NOTES.md の「リリースタイミングの指標」を参照


## 作業開始時の注意(セッション間引き継ぎ)

複数セッションが入れ替わりで作業しているため、着手前に必ず: (1) `git status` で
未コミットの作業ブランチが残っていないか、(2) 対応するIssueの本文とコメントの
両方、(3) この進捗ログの直近エントリ、(4) オープンなPRの有無、を確認すること。
過去に、Issueコメントの要望を見落として逆方向の実装をマージしてしまった事例が
ある(#28)。指示や既存の方針と矛盾する状況に気づいたら、進める前に必ず確認する。

## リリース履歴
- v0.2.1(2026-09-17): 致命的バグ2件(Stripe決済URL誤記 #21、GitHub Issue送信のURL長超過 #29)
  を含む修正をリリース。内容ベースの判断基準に基づき、決済・不具合報告という基幹機能が
  機能していない状態を放置しないため即リリースと判断。あわせてUI改善(#22, #23, #28)、
  自動更新の通知バナー(#18、一部)も含む。リリース作業中にrelease.yml自体の不備
  (win-x64ランタイムのrestore漏れ)が発覚し、同じリリースで修正(#38)。
